# Copy to .env.local and fill in for local development.
# In production (Vercel), set these in the Vercel dashboard.

# FastAPI backend URL (no trailing slash)
# Leave blank to use the Vite proxy (recommended for local dev)
VITE_API_URL=

# WebSocket base URL — auto-detected from window.location when blank
VITE_WS_URL=
